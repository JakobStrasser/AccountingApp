@model AccountingApp.Models.Company

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Company</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="OfficialId" class="control-label"></label>
                <input asp-for="OfficialId" class="form-control" />
                <span asp-validation-for="OfficialId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Bokföringsår start</label>
                <input name="accountingYearStart" class="form-control" type="date" />
                <label class="control-label">Bokföringsår slut</label>
                <input name="accountingYearEnd" class="form-control" type="date" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
